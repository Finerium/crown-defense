# Secrets, never commit
.env
.env.*
!.env.example
secrets/
*.pem
*.key
!**/testdata/**/*.key
# Live demo credentials for the console (the repo is public, the model key spend is real)
KREDENSIAL-DEMO.md

# Claude local overrides
.claude/settings.local.json
.crown/snapshots/
.crown/last-stop.txt

# Worktrees (worker isolation)
worktrees/
.crown/worktrees/

# Node / build
node_modules/
dist/
build/
.next/
out/
coverage/
*.tsbuildinfo

# Tooling output
.playwright-mcp/
.vercel/
apps/*/.env.local

# OS / editor
.DS_Store
*.log
npm-debug.log*

# Local data volumes
.pgdata/
tmp/
.env*
